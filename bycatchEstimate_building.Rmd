---
title: "bycatchEstimate"
output: html_document
date: "2024-11-25"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
pacman::p_load(tidyverse, here, rstan)
```

Making some fake data to test that I have a beta regression working

probably actually needs to be: 
- subsample (observer) samples from a bernoulli distribution with probability theta
- theta is drawn from a beta distribution

```{r}
fake_trip<-rbeta(100, 1, 1000)

subSample<-

hist(y_fake)

N<-length(y_fake)

data.list<-list(y=y_fake,
                N=N)
```

Fit model

```{r}
fit<-stan(file="bycatchModel.stan",
          data=data.list,
          chains=4)

post<-extract(fit)

summary(fit)

hist(post$phi)
```


